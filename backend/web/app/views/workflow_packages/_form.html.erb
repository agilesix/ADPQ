<%= form_for(workflow_package) do |f| %>
  <% if workflow_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_package.errors.count, "error") %> prohibited this workflow_package from being saved:</h2>

      <ul>
      <% workflow_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workflow_id %>
    <%= f.text_field :workflow_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
