<%= form_for(knowledge_article) do |f| %>
  <% if knowledge_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(knowledge_article.errors.count, "error") %> prohibited this knowledge_article from being saved:</h2>

      <ul>
      <% knowledge_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :published %>
    <%= f.check_box :published %>
  </div>

  <div class="field">
    <%= f.label :workflow_step_id %>
    <%= f.text_field :workflow_step_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
